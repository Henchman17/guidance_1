name: guidance_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.9
  google_mlkit_text_recognition: ^0.11.0 # For OCR (recommended)
  # tesseract_ocr: ^0.3.2 # Uncomment if you want to use Tesseract specifically
  image_picker: ^1.1.2
  path_provider: ^2.0.11
  open_file: ^3.2.1
  universal_html: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/s_logo.jpg
    - assets/images/download.jpg
    - assets/images/school.png
    - assets/images/logonbg.png
    - assets/files/Request Slip.pdf
